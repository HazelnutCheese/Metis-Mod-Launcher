<UserControl x:Class="ModEngine2ConfigTool.Views.Controls.TextField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ModEngine2ConfigTool.Views.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="FieldControl"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="240" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   VerticalAlignment="Bottom"
                   Text="{Binding FieldName, ElementName=FieldControl}" />
        <TextBox Grid.Column="1"
                 Margin="5,0,5,0"
                 Text="{Binding FieldValue, ElementName=FieldControl}" />
        <Button Grid.Column="2"
                Margin="5,0,0,0"
                Command="{Binding FieldAction, ElementName=FieldControl}"
                Content="{Binding FieldActionName, ElementName=FieldControl}" />
    </Grid>
</UserControl>
